{{- if .Values.secretFiles.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secretFiles.name }}
type: {{ .Values.secretFiles.type }}
stringData: 
  appconfig.json: {{ tpl (.Files.Get  .Values.secretFiles.filePath) . | quote }}
{{- end }}