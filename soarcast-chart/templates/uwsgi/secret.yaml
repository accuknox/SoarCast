{{- if .Values.uwsgi.secretFiles.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.uwsgi.secretFiles.name }}
type: {{ .Values.uwsgi.secretFiles.type }}
stringData: 
  appconfig.json: {{ tpl (.Files.Get  .Values.uwsgi.secretFiles.filePath) . | quote }}
{{- end }}